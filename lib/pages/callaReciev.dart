import 'package:flutter/material.dart';

class CallRec extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.orange,
      body: Center(
      child: Text("Home"),),
    );
  }
}
